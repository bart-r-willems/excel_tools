# excel_tools
Tools to be used in Python in conjunction with win32com

Frequent activities in Excel that can only be done through the `win32com` interface:
* Interact with pivot tables
* Convert `.xls` and `.xlsb` to `.csv` and/or `.xlsx`

